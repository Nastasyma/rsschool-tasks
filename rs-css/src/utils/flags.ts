import { IFlagsObject } from '../interfaces/flagsInterface';

const flagsObject: IFlagsObject = {
  level: 0,
  rule: false,
  game: {
    gameTable: null,
    burger: null,
  },
};

export default flagsObject;
